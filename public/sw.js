// Service Worker base. Necessario per far comparire il prompt di installazione su Android.
self.addEventListener('fetch', (event) => {
  // Pass-through: non fa nulla di particolare, ma soddisfa i requisiti PWA
});